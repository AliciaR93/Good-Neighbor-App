(function() {
  var messageComponent = {

  templateUrl: "templates/message.html"
  // controller: 

  // $location.path("/select");
}


  angular
  .module("GoodNeighborApp")
  .component("messageComponent", messageComponent);
  //^name of component in quotes, name of component not in quotes
}());
